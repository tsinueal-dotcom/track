import 'package:flutter/material.dart';

class AboutScreen extends StatelessWidget {
  const AboutScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('ስለ ኮሌጁ'),
        backgroundColor: Colors.deepPurple,
        foregroundColor: Colors.white,
      ),
      body: SingleChildScrollView(
        padding: const EdgeInsets.all(20),
        child: Column(
          crossAxisAlignment: CrossAxisAlignment.start,
          children: [
            Center(
              child: Container(
                width: 120,
                height: 120,
                decoration: BoxDecoration(
                  color: Colors.deepPurple.withOpacity(0.1),
                  shape: BoxShape.circle,
                ),
                child: const Icon(
                  Icons.school,
                  size: 60,
                  color: Colors.deepPurple,
                ),
              ),
            ),
            const SizedBox(height: 20),
            const Center(
              child: Text(
                'አሰንዳቦ ፖሊ ቴክኒክ ኮሌጅ',
                style: TextStyle(
                  fontSize: 24,
                  fontWeight: FontWeight.bold,
                ),
              ),
            ),
            const SizedBox(height: 8),
            Center(
              child: Text(
                'የቴክኒክ እና የሙያ ትምህርት ማዕከል',
                style: TextStyle(
                  fontSize: 16,
                  color: Colors.grey[600],
                ),
              ),
            ),
            const SizedBox(height: 30),
            _buildSectionTitle('የኮሌጁ ታሪክ'),
            _buildParagraph(
              'አሰንዳቦ ፖሊ ቴክኒክ ኮሌጅ በኦሮሚያ ክልል፣ በአሰንዳቦ ከተማ የሚገኝ የቴክኒክ እና የሙያ ትምህርት ኮሌጅ ነው። ኮሌጁ በብዙ የቴክኒክ እና የሙያ መስኮች ተማሪዎችን በማስተማር ላይ ይገኛል።',
            ),
            const SizedBox(height: 20),
            _buildSectionTitle('ዋና ዋና ግቦች'),
            _buildBulletPoint('የብቃት ቴክኒሺያኖችን ማፍራት'),
            _buildBulletPoint('የቴክኒክ ሙያን በተግባር ማስተማር'),
            _buildBulletPoint('የኢንዱስትሪ ፍላጎትን ማሟላት'),
            _buildBulletPoint('የሀገር ልማትን ማስተዋወቅ'),
            const SizedBox(height: 20),
            _buildSectionTitle('የትምህርት ደረጃዎች'),
            _buildBulletPoint('የመሰረታዊ የሙያ ትምህርት (Level I-IV)'),
            _buildBulletPoint('የዲፕሎማ ትምህርት'),
            _buildBulletPoint('የማህበራዊ ሳይንስ እና የቴክኒክ ትምህርቶች'),
            const SizedBox(height: 20),
            _buildSectionTitle('አካባቢ'),
            _buildParagraph(
              'ኮሌጁ በአሰንዳቦ ከተማ፣ ከአዲስ አበባ በስተደቡብ በሚገኝበት ቦታ ላይ ይገኛል።',
            ),
            const SizedBox(height: 30),
          ],
        ),
      ),
    );
  }

  Widget _buildSectionTitle(String title) {
    return Padding(
      padding: const EdgeInsets.only(bottom: 10),
      child: Text(
        title,
        style: const TextStyle(
          fontSize: 18,
          fontWeight: FontWeight.bold,
          color: Colors.deepPurple,
        ),
      ),
    );
  }

  Widget _buildParagraph(String text) {
    return Text(
      text,
      style: const TextStyle(
        fontSize: 15,
        height: 1.6,
        color: Colors.black87,
      ),
      textAlign: TextAlign.justify,
    );
  }

  Widget _buildBulletPoint(String text) {
    return Padding(
      padding: const EdgeInsets.only(bottom: 8),
      child: Row(
        crossAxisAlignment: CrossAxisAlignment.start,
        children: [
          const Icon(
            Icons.check_circle,
            color: Colors.deepPurple,
            size: 20,
          ),
          const SizedBox(width: 10),
          Expanded(
            child: Text(
              text,
              style: const TextStyle(fontSize: 15, height: 1.4),
            ),
          ),
        ],
      ),
    );
  }
}
